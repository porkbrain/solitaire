/**
 * The main application Vue object. The templates are loaded dynamically based
 * on current route from Controllers directory.
 *
 * @author Michael Bausano
 */

const app = new Vue({
  el: '#app',
})
